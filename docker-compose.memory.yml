services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      STORAGE: memory
      HTTP_ADDR: :8080
      LOG_OUTPUT: stdout
      CACHE_SIZE: 1000
    ports:
      - "8080:8080"